import React from "react";
import AddItem from "../components/AddItem/AddItem";

const CRUD = () => {
  return (
    // ? Страница для добавления новых элементов
    <div className="container">
      <AddItem />
    </div>
  );
};

export default CRUD;
